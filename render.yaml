services:
  - type: web
    name: flask-tracker-app
    env: python
    plan: free  # Use Render’s free tier; change if a different plan is needed.
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python server.py"
    envVars:
      - key: PORT
        value: 8000  # Render will set the actual port, so this serves as a default.
